name: Unit Tests (Node)
on:
  push:
    branches:
      - main
    paths:
      - 'custom-payment-flow/server/node/**'
      - '.github/workflows/node-unit-tests.yml'
  pull_request:
    paths:
      - 'custom-payment-flow/server/node/**'
      - '.github/workflows/node-unit-tests.yml'
  workflow_dispatch:

jobs:
  unit_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: custom-payment-flow/server/node
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: custom-payment-flow/server/node/package-lock.json
      - run: npm ci
      - run: npm test -- --coverage
